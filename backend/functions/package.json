{
  "name": "kids-oral-care-functions",
  "version": "1.0.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "admin:set": "node scripts/setAdminClaim.js",
    "consent:set": "node scripts/setParentalConsent.js",
    "usernames:backfill": "node scripts/backfillUsernameReservations.js"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
